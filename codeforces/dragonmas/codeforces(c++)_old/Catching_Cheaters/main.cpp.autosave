#include<bits/stdc++.h>
using namespace std;
int n,m;
string a,b;
int maxs(){
    cin>>n>>m>>a>>b;
    if(a.size()>b.size());
    swap(a,b);
    for(int i=0;i<b.size();i++){
        if(b[i]==a[j])j++,f=1;
        if(f)l++;
    }
}
int main(){
    cout<<maxs();
}